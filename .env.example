# Higgsfield generation runs through the Higgsfield MCP connector, which is
# authorized per-session via OAuth (in your Claude/agent client) — NOT via API
# keys in this file. So there are usually no secrets to set here.
#
# `requests` is used only to download finished result URLs (no auth needed).
#
# Never commit your real .env (it is gitignored).
