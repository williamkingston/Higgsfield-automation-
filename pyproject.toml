[tool.ruff]
line-length = 120
target-version = "py311"
# Vendored agent-skill scripts are not maintained here.
extend-exclude = [".agents"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
